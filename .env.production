# Public URL of the deployed Cloudflare Worker (see worker/yelp-proxy.js).
# NOT a secret (the URL is public) — safe to commit.
# Replace the placeholder after you deploy the worker, then run `npm run deploy`.
REACT_APP_YELP_PROXY=https://ravenous-yelp-proxy.justinpong0416.workers.dev
